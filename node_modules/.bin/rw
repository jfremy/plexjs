#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../railway/bin/railway" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../railway/bin/railway" "$@"
  ret=$?
fi
exit $ret
